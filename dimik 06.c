#include<stdio.h>
int main()
{
    int T,N,i,sum;
    scanf("%d",&T);

    for(i=0;i<T;i++)
    {
        scanf("%d",&N);
        int last_digit = N%10;

    for(N; N >= 10; N = N/10)
    {

    }
    sum = N + last_digit;

     printf("Sum = %d\n",sum);
    }



}
